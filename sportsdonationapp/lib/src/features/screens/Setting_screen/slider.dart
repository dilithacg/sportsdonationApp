import 'package:flutter/material.dart';

class SliderB extends StatelessWidget {
  const SliderB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Drawer(

    );
  }
}
